{
  "name": "@draft/web-image-proxy-api",
  "private": true,
  "version": "0.4.5",
  "description": "Image Proxy Service API",
  "scripts": {
    "dev": "pnpm build && ./draft-web-image-proxy-api",
    "clean": "pnpm build:clean",
    "deploy": "/bin/bash bin/deploy.sh fpm-application",
    "build": "exit 0 || go get . && go build",
    "build:docker": "docker build -t draft-web-image-proxy-api:latest -f Dockerfile .",
    "build:clean": "rm -f 'draft-web-image-proxy-api' '*.log'",
    "eslint": "exit 0",
    "flow": "exit 0",
    "test": "exit 0",
    "test:watch": "exit 0"
  }
}
